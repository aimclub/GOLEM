# heavy requirements needed for bandits and adaptive learning
torch
# karateclub has some outdated dependencies that sometimes
# can cause conflicts without a good reason.
# Its transitive gensim pin requires numpy < 2, which has no builds for
# Python 3.13+, so karateclub is unavailable there; the feather_graph
# context agent degrades gracefully without it.
karateclub>=1.3.3; python_version < '3.13'
